microservice-base:
  kubernetes:
    config:
      config-maps:
        - name: configmap-01
      secrets:
        - name: secret-01
          refresh-enabled: false
spring:
  application:
    name: secret
logging:
  level:
    com.freemanan.kubernetes.config: debug
