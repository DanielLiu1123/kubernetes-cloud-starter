microservice-base:
  kubernetes:
    config:
      namespace: default
      configmaps:
        - name: my-configmap
spring:
  application:
    name: normal
logging:
  level:
    com.freemanan.kubernetes.config: debug

username: freeman
hobbies:
  - coding